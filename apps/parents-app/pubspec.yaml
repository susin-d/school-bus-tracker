name: schoolbus_bridge_mobile
description: SURAKSHA Flutter app for parent and driver roles.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.9.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
